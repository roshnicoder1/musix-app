import { RowdirDirective } from './rowdir.directive';
describe('RowdirDirective', () => {
  it('should create an instance', () => {
    const directive = RowdirDirective;
    expect(directive).toBeTruthy();
  });
});