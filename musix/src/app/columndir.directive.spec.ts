import { ColumndirDirective } from './columndir.directive';
describe('ColumndirDirective', () => {
  it('should create an instance', () => {
    const directive = ColumndirDirective;
    expect(directive).toBeTruthy();
  });
});